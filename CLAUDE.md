# CLAUDE.md

`siakun/siakun` 프로필 저장소의 작업 규칙이다. 저장소 구성과 설계 근거는 `PROJECT.md`에 있다.

## 브랜치 전략: 트렁크 기반

`main`이 유일한 장수 브랜치다. `develop`이나 `feature/*`를 만들지 않고 `main`에 직접 커밋한다.

**전역 `CLAUDE.md`의 작업 브랜치 규칙(기본 브랜치에 직접 커밋하지 않고 develop으로 전환한다)은 이 저장소에 적용하지 않는다.** 푸시 정책(사용자가 직접 push한다)과 커밋 메시지 컨벤션은 그대로 따른다.

트렁크 기반을 고른 이유는 두 가지다.

- 기여자가 한 명이고 PR 리뷰 단계가 없다. 브랜치를 격리해 얻는 것이 없는 대신 머지와 rebase 비용만 남는다.
- GitHub Actions가 `main`에 이미지 산출물을 자동 커밋한다(대상과 스케줄은 `PROJECT.md` 1장과 2장 참고). 손 작업 브랜치를 오래 두면 그 브랜치가 자동 커밋과 계속 발산하고, 뒤늦게 정리하다 squash 전 커밋이 원격에 남는 식으로 히스토리가 엉킨다.

## 커밋 절차

1. `git pull --rebase origin main`으로 원격의 자동 커밋을 먼저 당긴다. 로컬 `main`을 `origin/main`의 연장선으로 유지하는 것이 이 저장소에서 가장 중요한 습관이다.
2. 변경을 커밋한다. 커밋 하나에 의도 하나를 담는다.
3. push는 사용자가 직접 실행한다.

생성기(`scripts/gen-*.sh`)를 고쳤으면 그 산출물도 같은 커밋에 넣는다. 산출물만 고치고 생성기를 두면 다음 재생성 때 되돌아간다.
